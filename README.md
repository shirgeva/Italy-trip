# Italy Trip

Personal Northern Italy trip planner. The site itself is kept as a single static `index.html` file.

Mobile navigation keeps the four primary trip views in the bottom bar, with search and secondary utilities available from the top controls.